# QGIS 旅行线路图制图规范与流程

本规范适用于博客中的区域旅行线路图、全国足迹图、全国铁路图和航线图。目标是让不同文章的地图保持同一套视觉语言，同时保留可编辑的 QGIS 工程和矢量数据。

当前版本：`2026-08-17`。本文以 `制图工具/scripts/maps` 中的现行脚本和 `地图输出` 中的发布工程为准，历史版本目录中的样式不再作为新图规范。

## 1. 信息层级

阅读顺序固定为：

1. 大标题与行程副标题。
2. 实际行程线、去过的城市名称。
3. 车站或机场节点。
4. 去过的行政区填色。
5. 未到访城市名称和低对比度行政区背景。

判断成图是否合格时，应把 PNG 缩到博客实际显示的约 `900-1100 px` 宽。缩小后首先应看到路线和城市名，其次是站点，最后才是行政区填色。

## 2. 标题与地名写法

- 大标题使用文章标题，或从文章标题提炼出的简短名称。
- 副标题只写具体行程，不写“行程轨迹”“路线图”等说明性文字。
- 地名之间使用 ` · ` 分隔。
- `某市（含某县）`：既去了该市市区，也去了所列县、县级市或镇。
- `某市某县`：只去了该地级市所辖的该县，没有把市区作为行程地点。
- 镇级地点沿用同一逻辑，例如 `龙岩上杭县古田镇`。
- 一般不在副标题中逐一写市辖区。确有必要时由具体文章单独指定。
- 标题区的行程日期统一写作 `YYYY.MM.DD—MM.DD`，例如 `2024.09.14—09.17`。
- 月、日均使用两位数字；结束日期即使与开始日期同月，也必须重复月份。
- 日期范围使用中文破折号 `—`，两侧不加空格；不使用半角连字符 `-`、短横线 `–` 或 `2024.09.14—17` 这类省略月份的写法。
- “数据截至”若只精确到月份，仍写作 `数据截至 YYYY.MM`，不套用日期范围格式。

示例：

```text
扬州（含高邮市） · 镇江
永州（含祁阳市） · 柳州 · 来宾 · 贵港 · 玉林 · 湛江
宣城泾县 · 福州 · 龙岩上杭县古田镇、长汀县 · 赣州（含瑞金市、于都县）
```

标题语义与地图高亮并不完全相同，不能根据括号机械生成所有高亮面。

## 3. 行政区高亮规则

- 到过某个地级市的任一县区，整座地级市均使用鼠尾草绿高亮。
- 县、自治县、县级市可再使用浅麦金作为第二层专题填色。
- 普通市辖区通常不单独使用浅麦金，因为整座地级市已经高亮。
- 台儿庄区、零陵区、冷水滩区是当前明确指定的例外，可单独使用浅麦金；以后按具体文章补充例外。
- 乡镇只标点或文字，不做乡级面高亮。
- 起点兼终点城市可使用灰粉棕，与一般去过城市区分。
- 需要强调的单向行程终点可使用低饱和蓝灰，与起点和一般游览城市区分。
- 路过但未停留游览的城市不高亮。

区域图推荐色值：

| 元素 | 颜色 | 建议透明度/线宽 |
|---|---|---|
| 无关区域 | `#F4F6F6` | 低对比度浅灰绿 |
| 去过的地级市 | `#D7E8E1` | 约 85% |
| 起终点城市 | `#DDB6AA` | 约 55% |
| 单向行程终点 | `#AFC9D6` | 约 55-60% |
| 重点县、县级市、例外市辖区 | `#E6D7A9` | 约 38-45% |
| 重点县区虚线 | `#2F6257` | 约 `0.18 mm` |
| 市界和省界 | `#A9B1B2` | 统一为 `0.15-0.16 mm` |

区域旅行图保留鼠尾草绿更合适：它与暗红车站、浅金县区、青绿色高铁形成稳定的综合色彩关系。蓝色系专用于全国足迹图，便于读者一眼区分“个人版图”和“单次行程”。不建议所有地图统一为蓝色，也不建议不同省份使用高饱和多色填充。

行政边界必须遵守以下规则：

- 专题填色面只负责填色，必须明确设置为 `NoPen`；不能用 `0 mm` 描边代替，否则 QGIS 仍可能输出黑色发丝线。
- 市界和省界由独立线图层绘制，颜色、线宽统一。区域图不再通过加粗省界建立层级，避免省边缘城市看起来整圈更重。
- 省界必须从当前市域数据按完整省份溶解生成，再按图幅显示；不能先筛选图幅内城市再溶解，否则普通市界会被误判为省界。
- 同一张图的填色、市界和省界必须使用同一套市域几何。发布工程不得同时叠加两套不同精度的 `city.json`、`province.json` 轮廓。
- 边界计算范围要覆盖地图框按长宽比扩展后的实际可见区域。现行脚本在请求范围外预留约 `25%` 候选余量，避免边缘城市有填色却缺少边界。
- 多部件面的边界要逐环提取，不能强制把分离的面转换成一条连续线，否则部件之间可能出现横贯图幅的连接直线。

## 4. 底图与投影

### 区域线路图

- 项目 CRS 使用 `EPSG:3857`，便于统一距离、比例尺和现有脚本输出。
- 当前区域图不使用 OSM 瓦片，也暂不显示山脉、河流等地理要素。
- 未到访地区使用低对比度行政区填色、统一边界和浅色地名提供空间参照。
- 区域图只显示实际到访铁路线路，不显示未乘坐的背景铁路网。
- 底图保持安静、可扫描，避免浅色 OSM 与高亮城市叠加后既看不清又增加杂乱信息。

### 全国足迹图、全国铁路图和航线图

- 使用中国 Albers 等积圆锥投影：

```text
+proj=aea +lat_1=25 +lat_2=47 +lat_0=0 +lon_0=105 +datum=WGS84 +units=m +no_defs +type=crs
```

- 该投影比直接使用经纬度更接近常见中国地图比例，不会显得过分扁平。
- 全国图以行政区矢量为底，不必使用 OSM。
- 南海诸岛使用固定右下角插图，插图底边与主图底边对齐，名称放在插图框内右下角。

## 5. 铁路数据与线路真实性

- 线路中心线优先来自本地铁路 OSM 数据或现有铁路 GeoPackage。
- 每一段乘车记录单独保存，至少包含 `seq`、`origin`、`destination`、`train`、`service` 字段。
- `service` 统一为 `highspeed` 或 `conventional`。
- `service` 表示车次统计与制图样式分类；选路使用独立的 `preferred_network`，表示实际优先经过高铁线还是普速线。两者不得复用同一字段。
- 动车走既有普速铁路时，几何仍沿实际普速线，但线路颜色和次数、里程统计继续归入高铁/动车。混合径路必须分段设置控制点，不能用一个全程选路类别代替各段真实线路。
- D901 北京西至广州北主要走京广高铁，经郑州东；广州北至广州站再经花广联络线和既有线进站。不能因末段进入既有线而把全程画在京广普速铁路上。
- 构建后必须逐条比对输出 `service` 与原始记录分类，并校验分项次数和里程，防止特殊径路改变统计或着色。
- 高铁和普铁平行时必须沿真实对应线路分别绘制，不能用两站间直线替代。
- 枢纽、联络线和已知绕行路线要加入控制点；文章记录与真实径路冲突时先复核，不凭外观猜测。
- 公路、轮渡等非铁路交通不套用铁路样式；市内景点之间不连线。
- 铁路路线默认不生成方向箭头；航线等需要表达方向的专题图由脚本生成独立箭头图层，并在导出前检查位置和旋转角度。

## 6. 铁路样式

区域图与全国铁路图使用同一套样式：

### 高铁/动车

- 外层白线：`1.30 mm`。
- 内层青绿线：`#2E7D83`，`0.72 mm`。
- 圆头、圆角。

### 普铁

- 外层深灰黑：`#283237`，`1.12 mm`。
- 内层暖白：`#FFFDF7`，`0.44 mm`。
- 内层使用连续线，形成稳定的双轨留白效果；不再使用黑白短划线。

### 站点

- 不区分重点站和普通站。
- 暗红圆点，白色细描边；点位应克制，避免压住路线。
- 区域图车站名默认使用思源黑体 CN 中等字重，约 `8.2 pt`，主体色 `#202020`。
- 白色缓冲只用于分离线路和边界，控制在约 `0.28 mm`，不能形成明显发光或阴影效果。
- 车站名优先放在车站点附近；只有确有遮挡时才使用独立标签点微调，不能移动真实站点。

图例用真实线路符号绘制，并只显示当前地图实际包含的类型：

```text
高铁/动车
普铁
```

区域图的高铁、普铁图例放在同一行；只有全国铁路图在统计口径需要说明时使用“高铁/动车（含城际、市郊）”。图例和比例尺必须在主图框外。比例尺使用 `Line Ticks Up`，不使用填充式黑白比例尺。

## 7. 标签

- 大标题：华文新魏，黑色；区域图约 `21-24.5 pt`，全国图约 `27 pt`。
- 副标题：华文楷体，黑色；区域图约 `9-9.5 pt`，全国图约 `9 pt`。
- 已到访城市名：华文新魏，约 `9.2-10.8 pt`，深色主体文字。
- 未到访城市名：同样使用华文新魏，约 `7.2 pt`，颜色 `#8A9390`，保持次级层次但不能更换字体。
- 所有主体文字使用 `#202020`；括注和“未入”等备注使用 `#555555`，括号内文字比主体小一个字号。
- 车站名使用思源黑体 CN 中等字重，主体色 `#202020`，白色缓冲保持克制。
- 全国图不显示省名时，只保留需要辨认的城市名。
- 未到访城市名称使用市域多边形标签，启用 `fitInPolygonOnly`，并关闭 `displayAll` 和 `allowDegradedPlacement`；整段文字必须留在本市范围内。
- 标签候选范围必须覆盖地图框实际显示的边缘余量。不能因为标签锚点位于原始请求范围外而漏掉画面中的城市名。
- 当边缘可见市域不足以容纳文字时，优先扩展候选范围或微调地图范围；不能用强制显示把地名压到相邻城市。
- 车站、机场、景点和确需手调的已到访城市可使用独立点标签层；移动的是标签锚点，不是真实几何。
- 任何标签都不能越出地图边缘、越出所属行政区或覆盖相邻站点。

## 8. 行程卡片

- 甘肃图和通用区域图共用同一套卡片布局逻辑，不保留两套字号、圆角、内边距或虚线参数。
- 每个地点单独作为一行标签，使用固定行高和垂直居中；不能再用多行文本框估算总高度。
- 卡片高度从最后一行文字的实际位置反推，底部统一保留约 `1.8 mm` 视觉边距。
- 标题和日期使用粗体；景点正文使用常规或中等字重，避免整块文字过黑。
- 主体文字使用 `#202020`；括注、“未入”等备注使用 `#555555`，括号内文字小一个字号。
- 卡片宽度按最长一行的实际宽度计算，使最长行到左右边界的视觉距离相同；卡片不能贴住或超出主图右边界。
- 统一使用紧凑圆角、内边距和浅色分隔虚线，不在卡片内嵌套第二层卡片。
- 引线连接卡片最近的边缘，并优先采用短折线；避免先画很长的水平线再折向地点。
- 引线、卡片和地名不能遮挡车站、路线或重要行政边界。

## 9. 航线图

- 页面和地图使用深蓝黑背景，省界和省名保持低对比。
- 航线使用浅青色发光双层线，机场为高亮圆点。
- 每次飞行是一条平滑贝塞尔弧线，重复航线轻微错位。
- 每条航线在目的地机场前绘制一个沿弧线方向的小箭头；箭头应轻于机场节点和航线，不遮挡机场名称。
- 经停航线仍是一条连续弧线，并精确经过经停机场；经停机场与其他机场使用同一套节点颜色。
- 航班次数、总里程、城市数、机场数放在主图框外，不制作 App 顶部统计条。
- 航线图不放图例，机场角色通过节点颜色和必要的地名直接表达。

当前统计文案：

```text
15 次飞行｜累计 21,281 km｜抵达 12 座城市、13 座机场（含经停）
```

## 10. 布局与导出

- 页面四周保留统一页边距，主图框使用 `0.22-0.24 mm` 灰色线。
- 标题区、主图区、页脚区严格分开。
- 比例尺及存在时的图例放在页脚区，不能压入主图；页脚文字使用粗体并按网页缩略图检查可读性。
- 区域范围只保留说明路线所需的上下文，避免东西或南北出现大块无信息空白。
- 边缘城市的可见轮廓、边界和地名必须一致；不能出现城市已经进入画面但边界或地名未参与计算的状态。
- QGIS 地图框会为匹配长宽比自动扩展请求范围。边界和标签候选范围必须覆盖这部分实际可见余量；必要时先调整页面长宽比，再微调地图范围。
- PNG 优先沿用原导出分辨率；仅当成图超过 `3 MB` 时降低导出 DPI。当前高复杂度区域图使用 `210 dpi`，其中“回家路上1”和“回家路上2完整路线”使用 `190 dpi`。保留同名 `.qgz` 与专题 `.gpkg`。
- `.qgz` 必须保存项目默认视图范围和项目主目录；打开后主画布应直接定位到成图范围，而不是空白或全球范围。
- 发布前检查博客宽度缩略图和原尺寸图两种状态。

## 11. 自动化目录

整理后的目录：

```text
F:\Desktop\Railway\
├─ 制图工具\
│  ├─ scripts\maps\
│  ├─ 数据源\
│  │  ├─ GeoPackage\
│  │  ├─ Shapefile\
│  │  └─ OSM\
│  ├─ 日志\
│  ├─ 临时数据\
│  ├─ artifacts\
│  └─ symbology-style.db
├─ 地图输出\
│  ├─ 全国专题图\
│  ├─ 区域线路图\
│  └─ 历史版本\
├─ city\
├─ province\
└─ 旧图\铁路底图_20260101\
```

行政区源数据保留在 `city`、`province`，铁路基础数据统一放在 `制图工具/数据源`。区域图以市域数据作为填色和边界的共同几何来源，省界由完整市域集合派生；`province` 中的独立轮廓只作为源数据或历史兼容数据，不能与市域轮廓叠加到同一发布工程。现行构建脚本使用新路径，重新导出的发布工程不得引用根目录散落的 `.gpkg`、`.shp` 或 `.pbf`。

## 12. 用代码操作 QGIS

脚本通过 QGIS 自带 Python 启动，创建图层、设置符号、创建打印布局并导出 PNG。不要使用普通系统 Python 直接运行含 `qgis.core` 的脚本。

PowerShell 示例：

```powershell
& 'E:\QGIS 3.44.8\bin\python-qgis-ltr.bat' `
  'F:\Desktop\Railway\制图工具\scripts\maps\regional_overviews\build_overviews.py'
```

只重建一张区域图：

```powershell
& 'E:\QGIS 3.44.8\bin\python-qgis-ltr.bat' `
  'F:\Desktop\Railway\制图工具\scripts\maps\regional_overviews\build_overviews.py' `
  --only home3
```

航线图：

```powershell
& 'E:\QGIS 3.44.8\bin\python-qgis-ltr.bat' `
  'F:\Desktop\Railway\制图工具\scripts\maps\flight_history\build_flight_map.py'
```

“走河西”区域图：

```powershell
& 'E:\QGIS 3.44.8\bin\python-qgis-ltr.bat' `
  'F:\Desktop\Railway\制图工具\scripts\maps\gansu_hexi\build_map.py'
```

“山陕漫游”和“甘肃行旅”使用各自脚本，但边界、标签和填色规则必须与通用区域图一致：

```powershell
& 'E:\QGIS 3.44.8\bin\python-qgis-ltr.bat' `
  'F:\Desktop\Railway\制图工具\scripts\maps\shanxi_shaanxi_redesign\build_redesign.py'

& 'E:\QGIS 3.44.8\bin\python-qgis-ltr.bat' `
  'F:\Desktop\Railway\制图工具\scripts\maps\gansu_itinerary\build_map.py'
```

上大学前去过的城市图：

```powershell
& 'E:\QGIS 3.44.8\bin\python-qgis-ltr.bat' `
  'F:\Desktop\Railway\制图工具\scripts\maps\pre_university\build_map.py'
```

脚本导出后再运行同目录的 `validate_*.py`。校验至少覆盖：图层有效性、路线和站点数量、主图/南海插图数量、图例与比例尺位置、PNG 分辨率。区域图还要检查：

- 发布工程没有 OSM 和未乘坐铁路背景图层。
- 填色面无描边，边界只由统一线图层绘制。
- 发布工程没有同时引用独立的 `province.json` 和市域边界。
- 未到访城市标签启用市域约束，边缘城市的边界与地名均完整参与计算。

全部当前工程的默认视图、相对路径和方向图层可见性可统一检查；航线图要求航向箭头可见，其他辅助方向图层默认隐藏：

```powershell
& 'E:\QGIS 3.44.8\bin\python-qgis-ltr.bat' `
  'F:\Desktop\Railway\制图工具\scripts\maps\validate_projects_openable.py'
```

如果打开旧工程时主画布看似空白，先打开“项目 > 布局管理器”查看已经保存的打印布局，或在图层面板右击“实际铁路行程”并选择“缩放到图层”。这通常不是数据丢失，而是旧脚本只保存了布局范围，没有保存 QGIS 主画布的默认范围。新版脚本会写入 `setDefaultViewExtent`、`setPresetFullExtent` 和项目主目录；数据源同时使用相对路径，移动整个输出文件夹后仍可正常读取。

## 13. 手工调整位置

地点真实位置与文字显示位置要分开处理：

1. 车站、机场、景点几何保持真实经纬度。
2. 未到访城市名默认使用受市域约束的多边形标签，不使用手工点位强制显示。
3. 车站名、景点名和确需微调的已到访城市名可使用独立标签点图层；移动的是标签锚点，不是行政区边界或站点。
4. 在 QGIS 中启用标签工具栏，使用“移动标签”微调；或直接编辑专用标签点图层。调整后必须复查文字没有越出所属行政区和地图框。
5. 线路必须捕捉到真实铁路中心线；仅为避免文字冲突时移动文字，不移动线路。

这样既保证坐标真实，也能保持最终版面清晰。
